# AGENTS.md — StockManagement 開発エージェント指示書

> このファイルは Codex CLI（および AGENTS.md 対応エージェント）が起動時に自動で読み込む恒久的な指示書です。
> Claude Code 時代の `CLAUDE.md` と、プロジェクト固有メモリ（コミュニケーション・コーディング・開発ペースの好み）を 1 つに統合しています。
> Codex は Git ルートから現在ディレクトリまでの各階層の `AGENTS.md` を連結して読み込みます（合計 32 KiB 上限）。

---

## あなたの振る舞い

- ユーザーは時間が十分にあるので、あなたが出せるベストを尽くして回答してください。
- **必ず敬語で返答すること。**
- **体言止めを使わないこと。** 「~なので。」のような言い方も禁止。文は「〜です」「〜ます」「〜してください」などで終える。
- **日本語でやりとりすること。** ただしコミットメッセージ・PRタイトル・GitHub Issues のタイトルは英語（conventional commits 形式）。Issues 本文は日本語可。

### フィードバックへの向き合い方（最重要）

ユーザーがスクリーンショット付きで問題報告やフィードバックをおこなった場合、必ず内容を受け止めて具体的な対応を示してください。「それはさておき」「確認できましたか？」のように流すのは厳禁です。

- **Why:** ユーザーが時間をかけて確認・報告してくれている。それを軽視する態度は信頼を損なう。
- **対応の型:** (1) 問題の原因を分析する、(2) 今すぐ直せるなら直す、(3) 今すぐ直せないならタスクとして明確に記録する。

### 開発ペースの好み

ユーザーは開発のペースを重視しています。各ステップでの細かい確認を最小限にして、**ストーリー作成 → 実装 → コードレビュー**までを一気通貫で進めることを好みます。実装後のコミットとプッシュについては、後述の Git 操作ルールに従ってください。

ただし以下の「事前確認が必須の変更」（後述）は例外で、必ず先に確認してください。

---

## プロジェクト概要

- **プロジェクト名**: StockManagement
- **概要**: 株式投資の財務分析 Web アプリ（スプレッドシートからの移行）
- **対象**: 中長期保有の個人投資家向け
- **開発者**: Fujita Kenshirou（個人開発、平日 1h ＋休日 2-3h の趣味ペース）
- **開発手法**: BMAD-METHOD によるアジャイル AI 駆動開発
- **技術スタック**: Next.js（App Router）+ Supabase + Vercel（月額 ¥0 運用）。TypeScript / React 19 / Tailwind CSS v3.4 / shadcn-ui / Vitest。
- **プロダクトブリーフ**: `_bmad-output/planning-artifacts/product-brief-StockManagement-2026-03-03.md`

### 参考資料（ドメイン知識）

- 理論株価の計算手法: 山口揚平氏の書籍ベース
- 参考書籍: 「株式投資 第6版」（シーゲル）
- 既存スプレッドシート: Google Spreadsheet（12 タブ構成）
- 適正株価の考え方: ファイナンス理論（別途資料あり）

---

## 事前確認が必須の変更

ペース優先で進めてよいですが、以下のカスタマイズ・設定変更は影響が大きいため、**実装前に必ずユーザーへ確認**してください。

- DB スキーマ変更（テーブル / カラム / 制約）
- 認証方式の変更
- 外部 API の追加・変更（EDINET、株価、AI API 等）
- 権限設定（Codex の `config.toml`、Supabase RLS 等）
- デプロイ設定（Vercel 環境変数、ビルド設定）
- 既存計算ロジックの仕様変更（端数処理・単位・期ズレを含む）

---

## 開発フェーズ

```
1. 分析（任意） → 2. 計画 → 3. ソリューショニング → 4. 実装 → エピック振り返り
```

- 実装前に未知の技術があれば必ずリサーチフェーズを挟む。
- タスク規模と既存成果物に応じた開始地点は `bmad-help` の案内に従う。
- 時間は十分にあるので、ベストな解決策を見つける。
- 開発ステップは小分けにする。ただし、ユーザーから明示的な指示がない限りコミットやプッシュはせず、未コミットの差分として残す。
- 次に何をすべきか迷ったら BMAD の `bmad-help` スキル（Codex では `$bmad-help`）を参照する。

---

## コーディング規約

### 開発スタイル（最重要）

- **小さく切り出す**: 変数・定数・関数・モジュールはできる限り小さく切り出し、アジャイルな変更に対応できるようにする。1 つの関数は 1 つの責務に絞る。
- **コメントを書く**: 人間の認知負荷を下げるため、変数・定数・関数などの意図や目的を説明するコメントを必ずつける。「何をしているか」ではなく「**なぜそうしているか**」を書く。モジュール冒頭にはそのファイルの責務を記述する。
  - **Why:** ユーザーはコードレビューを自分で行い、AI が生成したコードの意図を理解したい。大きな関数や無名の定数は認知負荷が高い。
  - 既存コードを変更するときも、このスタイルに合わせる。

### 基本ルール

- テストは実装と同時に書く（TDD 推奨）。テストランナーは Vitest。
- `.env` や秘密情報はコミットしない（`.env.local` はローカル専用）。
- セキュリティ（OWASP Top 10）を意識したコードを書く。
- セマンティック HTML を優先する。`<button>`, `<table>`, `<nav>`, `<main>`, `<label>` 等の標準タグを正しく使い、ARIA による後付けを最小限にする（WCAG 2.1 Level AA 準拠）。
- すべての主要指標は「数式」「入力参照（期・単位）」「端数処理」「calc_version」を表示できることを前提に実装する。
- 計算エンジンの変更は、既存のゴールデンテスト（1 銘柄 × 3 期）を必ず通す。差分が出た場合は、端数処理・単位・期ズレ・入力マッピングのいずれかとして理由を明記する。
- UI の PR では、最低限「キーボード操作」「フォーカス可視」「フォームエラーの読み上げ（`aria-describedby` 等）」をセルフチェックする。

---

## コミット規約

### Git 操作ルール（最重要）

- **ユーザーからその都度明示的な指示がない限り、コミットしない。**
- **ユーザーからその都度明示的な指示がない限り、プッシュしない。**
- 実装や修正の依頼、および「完了まで進めて」という依頼を、コミットやプッシュの許可として解釈しない。
- 作業完了時は原則として未コミットの差分を残し、変更内容と検証結果を報告する。
- コミットだけが指示された場合はプッシュせず、プッシュだけが指示された場合も対象ブランチとコミットを確認してから実行する。

Conventional Commits 形式:

```
<type>(<scope>): <description>

[optional body]

Co-Authored-By: Codex <noreply@openai.com>
```

**type**: `feat`, `fix`, `docs`, `style`, `refactor`, `test`, `chore`, `ci`

## ブランチ戦略

- `main` — 本番ブランチ（直接 push しない）
- `feature/<issue番号>-<簡潔な説明>` — 機能開発
- `fix/<issue番号>-<簡潔な説明>` — バグ修正
- `chore/<説明>` — メンテナンス作業

## Pull Requests

- 1 Issue = 1 PR を原則とする。
- PR 作成時は Issue 番号を紐付ける（`Closes #<番号>`）。

---

## よく使うコマンド

| 目的 | コマンド |
|---|---|
| 開発サーバー | `npm run dev` |
| ビルド | `npm run build` |
| Lint | `npm run lint` |
| テスト（1 回） | `npm run test` |
| テスト（watch） | `npm run test:watch` |
| フォーマット | `npm run format` |
| ローカル Supabase | `supabase start` / `supabase stop` |

---

## 技術メモ（設計時のインプット）

### データモデルの最小案

- `Company`（コード / 名称 / 市場 / 業種）
- `FinancialStatement`（companyId, period(年度/四半期), consolidated flag, currency）
- `FinancialMetricRaw`（売上 / 営業利益 / 税引後利益 / 総資産 / 自己資本 / 有利子負債 / 営業CF / 投資CF…）
- `ValuationAssumption`（成長率 / 割引率 / ターミナル等）
- `ValuationResult`（理論株価、ROIC 等の派生指標、計算バージョン）

### 透明性ログ（Phase 1 から入れる）

- `CalculationTrace`（指標名 / 数式文字列 / 入力参照 / 出力 / 端数処理 / バージョン）
- 「式」と「参照元」を画面で見せることが Phase 1 の差別化の核心。最初から一段だけ用意しておくと後で楽。

### 計算ロジックのズレ原因トップ 3（テストで最初に固定する）

1. **端数処理**（四捨五入 / 切上げ / 切捨て、表示桁）
2. **単位**（百万円 / 千円 / 円）
3. **期ズレ**（年度と四半期、連結 / 単体）

テストは「1 銘柄 × 3 期」で十分（カバレッジより差分検出の速さを優先）。

### Phase 2: EDINET 半自動の設計指針

1. EDINET から「候補値」を出す。
2. ユーザーが確定する（採用 / 修正 / 保留）。
3. 判断理由をログに残す（透明性）。
4. 勘定科目マッピングをログの主役にする（例:「この会社の '長期借入金' を有利子負債に含めた」）。このマッピングルール自体が資産になり、次回取得時に再利用できる。

---

## 既知の注意点

### shadcn/ui と Tailwind バージョンの不一致

shadcn/ui の新しいバージョンは Tailwind v4 構文（例: `w-(--sidebar-width)`）を出力しますが、本プロジェクトは **Tailwind v3.4.1** を使用しています。コンポーネントを追加・更新する際は、v4 構文が混入していないか確認し、必要なら `w-[var(--sidebar-width)]` のような v3 構文へ手動変換してください。

### 将来タスク: 銘柄情報の自動取得

銘柄登録時に、銘柄コードや会社名を入力するだけで企業名・市場・業種を自動取得したいという要望があります。外部 API（JPX 銘柄情報、Yahoo Finance 等）の技術リサーチが必要です。Epic 5 以降のバックログ候補です。

---

## BMAD ワークフロー

本プロジェクトは BMAD Method を開発プロセスの基盤として使用します。導入状態の正本は `_bmad/_config/manifest.yaml` で、フレームワーク本体は `_bmad/`、成果物は `_bmad-output/` に保存します。

- 新しい開発タスクでは、ユーザーが使用するスキルを指定していない限り、最初に `bmad-help` で現在の成果物と推奨フローを確認する。
- Codex では `.agents/skills/` のスキルを `$bmad-help`、`$bmad-quick-dev`、`$bmad-code-review` のように呼び出す。
- Claude Code では `.claude/skills/` に生成された同等のスキルを使用する。
- 小規模な修正は Quick Flow、大規模な機能追加や設計変更は分析・計画・アーキテクチャ・エピック・実装の各スキルを順に使用する。
- `_bmad-output/` にある既存の企画・設計・スプリント成果物を入力として読み、更新対象でない成果物を上書きしない。
- `_bmad/`、`.agents/skills/`、`.claude/skills/` はインストーラー管理のため直接編集しない。恒久的なチーム設定は `_bmad/custom/config.toml` に記述する。
- Quick Dev と Dev Auto の Git 操作制限は `_bmad/custom/bmad-quick-dev.toml` と `_bmad/custom/bmad-dev-auto.toml` に定義している。各ワークフロー標準の自動コミット手順より、このプロジェクト設定を優先する。
- Dev Auto は完了状態をコミットSHAで管理するため、その会話内でローカルコミットまで明示的に許可された場合だけ使用する。通常の未コミット運用では Quick Dev を使用する。
- BMAD の更新には公式インストーラー `npx bmad-method install` を使用し、Codex と Claude Code の両方を連携対象として維持する。
- BMAD 更新後は `_bmad/core/config.yaml` と `_bmad/bmm/config.yaml` の両方で `project_name: StockManagement` が解決できることを確認する。
- Python を使用する BMAD スキルは `uv run` を前提とする。`uv` がない環境では、実行前にユーザーへ環境導入の許可を確認する。
- スキル本文中の `{project-root}` はこのリポジトリのルート（Git ルート）を指す。
