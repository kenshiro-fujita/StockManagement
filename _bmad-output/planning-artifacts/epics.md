---
stepsCompleted: [1, 2, 3, 4]
inputDocuments:
  - "prd.md"
  - "architecture.md"
  - "ux-design-specification.md"
---

# StockManagement - Epic Breakdown

## Overview

This document provides the complete epic and story breakdown for StockManagement, decomposing the requirements from the PRD, UX Design, and Architecture requirements into implementable stories.

## Requirements Inventory

### Functional Requirements

**銘柄管理:**
- FR1: ユーザーは銘柄を新規登録できる（銘柄コード、企業名、市場、業種、事業セグメント）
- FR2: ユーザーは登録済み銘柄の情報を編集・削除できる
- FR3: ユーザーは登録済み銘柄の一覧を閲覧できる
- FR4: ユーザーは銘柄一覧で各銘柄の理論株価・主要指標を一目で確認できる
- FR5: ユーザーは銘柄をロースターカテゴリに分類できる *[Phase 3]*
- FR6: ユーザーはロースター分類の変更履歴と理由を記録・閲覧できる *[Phase 3]*
- FR7: ユーザーはAIによる当該銘柄の調査結果を取得・閲覧できる *[Phase 4]*

**財務データ管理:**
- FR8: ユーザーは銘柄ごとに四半期・年度の財務データを手動入力できる
- FR9: ユーザーは入力済み財務データを修正できる
- FR10: ユーザーは銘柄ごとの財務データの推移（複数期分）を閲覧できる
- FR11: システムは入力値に対して基本的な妥当性チェックを行う
- FR12: ユーザーは財務データの期間属性（年度/四半期、連結/単体）を指定できる
- FR13: システムはEDINET等から主要な財務データを自動取得できる *[Phase 2]*
- FR14: システムは自動判定が困難な項目について候補値を提示し、ユーザーが確認・修正できる *[Phase 2]*
- FR15: システムはデータ判定の過程をログとして記録・表示できる *[Phase 2]*
- FR16: システムは過去の勘定科目マッピング判定を再利用できる *[Phase 2]*
- FR17: システムは前期と異なる勘定科目マッピングを検出した場合にアラートを出せる *[Phase 2]*

**計算エンジン — 前提パラメータ:**
- FR18: ユーザーは銘柄ごとに前提パラメータを設定・調整できる（r, g, 実効税率等）
- FR19: システムはパラメータのデフォルト値を提供し、ユーザーはそのデフォルト値と根拠を確認できる
- FR20: システムはパラメータ変更時に関連するすべての指標を即座に再計算できる

**計算エンジン — 自動算出指標:**
- FR21: システムは以下の指標を財務データとパラメータから自動算出できる：自己資本比率、純利益率、売上営業利益率、前年比成長率、営業CF/投資CF/FCF、ROE/ROA/ROIC/移動平均ROIC、PER/PBR/EPS、現状事業価値/現状資産価値、現状理論株価/成長込理論株価、理論PER/理論時価総額/5年後理論時価総額/6年目当期純利益、安全域/安全率、理想購入株価

**計算エンジン — 銘柄詳細ビュー:**
- FR22: ユーザーは銘柄ごとに理論株価関連の全指標を一覧で閲覧できる
- FR23: システムは理論株価と現在の市場価格を比較表示できる

**銘柄比較ビュー:**
- FR24: ユーザーは複数銘柄を横並びに比較できる *[Phase 3]*
- FR25: ユーザーは各銘柄の5段階評価を手動で設定できる *[Phase 3]*
- FR26: ユーザーは購入優先順を手動で設定できる *[Phase 3]*

**透明性・計算ロジック閲覧:**
- FR27: ユーザーはすべての計算ロジック（数式、理論的根拠）を閲覧できる
- FR28: ユーザーは各指標の算出に使用された入力値と数式の参照関係を追跡できる

**データ取得自動化:**
- FR29: システムはEDINET APIと連携して有価証券報告書の主要財務項目を取得できる *[Phase 2]*
- FR30: システムはXBRLデータから必要な財務項目を抽出・マッピングできる *[Phase 2]*

**定性分析:**
- FR31: システムは銘柄の事業概要・セクター・主要事業を表示できる *[Phase 4]*
- FR32: システムはIR資料から事実を整理して提示できる *[Phase 4]*
- FR33: システムは定性分析において事実の提示に徹し、スコアリングは行わない *[Phase 4]*

**認証・アカウント:**
- FR34: ユーザーはアカウントを作成してログインできる
- FR35: ユーザーのデータはアカウントに紐づいて保存される

**Phase 1 スコープ: FR1-4, FR8-12, FR18-23, FR27-28, FR34-35**

### NonFunctional Requirements

**パフォーマンス:**
- NFR1: 1銘柄あたりの理論株価算出は3秒以内に完了すること
- NFR2: 50銘柄以上の一覧表示は1秒以内にレンダリングされること
- NFR3: 初回ページロードは3秒以内に完了すること
- NFR4: パラメータ変更時、関連指標の再計算が1秒以内に画面に反映されること
- NFR5: EDINET データ取得はバックグラウンド処理とし、UIをブロックしないこと *[Phase 2]*

**セキュリティ:**
- NFR6: すべての通信はHTTPS（TLS 1.2以上）で暗号化されること
- NFR7: Supabase の RLS を Phase 1 から有効にし、認証済みユーザーが自身のデータのみにアクセスできること
- NFR8: 認証トークンはセキュアに管理し、クライアントサイドに機密情報を露出しないこと
- NFR9: OSS公開時、ユーザーのポートフォリオ・投資判断データが開発者側に一切渡らないアーキテクチャとすること

**アクセシビリティ:**
- NFR10: Phase 1 から WCAG 2.1 Level AA に準拠すること
- NFR11: すべてのインタラクティブ要素がキーボードのみで操作可能であること
- NFR12: スクリーンリーダーで全機能が利用可能であること
- NFR13: 主要操作に対するキーボードショートカットを提供すること
- NFR14: 色のみに依存する情報伝達を行わないこと
- NFR15: テキストと背景のコントラスト比は WCAG AA 基準を満たすこと

**インテグレーション:**
- NFR16: EDINET API のアクセス頻度制限を遵守すること *[Phase 2]*
- NFR17: 外部API障害時にユーザーに明確なエラーメッセージを表示し、手動入力へのフォールバックを提供すること
- NFR18: 外部APIリクエストのタイムアウトは30秒以内、リトライは最大3回まで

**信頼性:**
- NFR19: Supabase のデータベースバックアップを有効にすること
- NFR20: 計算エンジンのロジック変更時に既存データの整合性が保たれること
- NFR21: 障害発生時のデータ復旧手順をドキュメント化すること

### Additional Requirements

**Architecture からの技術要件:**
- スターターテンプレート: `create-next-app --example with-supabase` + `npx shadcn@latest init` — Epic 1 Story 1 で初期化する
- データモデル: ワイドテーブル設計（stocks, financial_data, parameters テーブル）、全テーブルに user_id + RLS
- 単位保存: 円に統一して保存。入力時の元単位はメタデータとして input_unit カラムに保存する
- 計算エンジン: クライアントサイド純粋関数（lib/calc/）。透明性メタデータ（数式・入力参照・端数処理・calc_version）を結果と一緒に返す
- バリデーション: Zod スキーマを lib/schemas/ に配置。フロント/サーバーで共有する
- データアクセス: 読み取り = Server Components、書き込み = Server Actions、再取得 = revalidatePath
- テスティング: Vitest でゴールデンテスト（1銘柄×3期でスプレッドシートとの一致検証）
- CI/CD: GitHub Actions（型チェック + ESLint + Vitest + ビルド）、Vercel 自動デプロイ
- フォーマッター: Prettier + Tailwind CSS プラグイン
- 環境変数: .env.local（Git管理外）+ .env.example（Git管理）

**UX Design からの要件:**
- デザインシステム: shadcn/ui + Tailwind CSS。CSS変数ベースのテーマシステム
- レイアウト: タブ整理型ベース + サイドバー理論株価サマリー。サイドバー240px固定 + メインコンテンツ（フルード）
- カスタムコンポーネント: CalcLogicPanel, StockBanner, TheoryPriceSummary, FinancialDataForm, ParameterAdjustPanel
- ナビゲーション: サイドバー（グローバルナビ + 銘柄リスト + 理論株価サマリー）+ タブ（概要/理論株価/財務データ/パラメータ）
- フィードバック: Toast通知（保存成功）、インラインハイライト（指標更新）、アンバーバッジ（前期比大幅変動）
- フォーム: onBlur バリデーション、明示的保存（Ctrl+S対応）、単位選択ドロップダウン
- 透明性パターン: クリッカブル指標（ティール色テキスト + 点線下線）→ CalcLogicPanel インライン展開
- レスポンシブ: デスクトップファースト。lg(1024px)以上がフル体験、md(768px)以下はサイドバー非表示
- アクセシビリティ: セマンティックHTML、キーボード操作、フォーカス可視、フォームエラーの aria-describedby
- ローディング: スケルトンUI（スピナー不使用）、部分ロード（サイドバー+バナー即時表示、タブコンテンツはスケルトン）
- Empty States: 銘柄未登録時/財務データ未入力時/理論株価未算出時のガイドテキスト

### FR Coverage Map

- FR1: Epic 2 — 銘柄新規登録
- FR2: Epic 2 — 銘柄編集・削除
- FR3: Epic 2 — 銘柄一覧閲覧
- FR4: Epic 4 — 一覧での理論株価・主要指標表示
- FR5: Epic 6 — ロースター分類 *[Phase 3]*
- FR6: Epic 6 — ロースター変更履歴 *[Phase 3]*
- FR7: Epic 8 — AI調査結果 *[Phase 4]*
- FR8: Epic 3 — 財務データ手動入力
- FR9: Epic 3 — 財務データ修正
- FR10: Epic 3 — 財務データ推移閲覧
- FR11: Epic 3 — 入力値妥当性チェック
- FR12: Epic 3 — 期間属性指定
- FR13: Epic 5 — 財務データ自動取得 *[Phase 2]*
- FR14: Epic 5 — 候補値提示・確認 *[Phase 2]*
- FR15: Epic 5 — データ判定ログ *[Phase 2]*
- FR16: Epic 5 — マッピング判定再利用 *[Phase 2]*
- FR17: Epic 5 — マッピング変更アラート *[Phase 2]*
- FR18: Epic 4 — 前提パラメータ設定
- FR19: Epic 4 — デフォルト値と根拠
- FR20: Epic 4 — パラメータ変更時再計算
- FR21: Epic 4 — 指標自動算出
- FR22: Epic 4 — 理論株価全指標一覧
- FR23: Epic 4 — 市場価格比較表示
- FR24: Epic 7 — 複数銘柄比較 *[Phase 3]*
- FR25: Epic 7 — 5段階評価 *[Phase 3]*
- FR26: Epic 7 — 購入優先順 *[Phase 3]*
- FR27: Epic 4 — 計算ロジック閲覧
- FR28: Epic 4 — 入力値・数式追跡
- FR29: Epic 5 — EDINET API連携 *[Phase 2]*
- FR30: Epic 5 — XBRLデータ抽出 *[Phase 2]*
- FR31: Epic 8 — 事業概要表示 *[Phase 4]*
- FR32: Epic 8 — IR資料整理 *[Phase 4]*
- FR33: Epic 8 — 定性分析（事実提示） *[Phase 4]*
- FR34: Epic 1 — アカウント作成・ログイン
- FR35: Epic 1 — データのアカウント紐付け

## Epic List

### Epic 1: プロジェクト初期化と認証基盤 *[Phase 1]*
ユーザーがアカウントを作成してログインし、認証済みのダッシュボードにアクセスできるようにする。プロジェクトの技術基盤（スターターテンプレート、DBスキーマ、RLS、CI/CD）を構築する。
**FRs:** FR34, FR35

### Epic 2: 銘柄管理 *[Phase 1]*
ユーザーが投資対象の銘柄を登録・編集・削除し、ウォッチリストとして一覧表示できるようにする。将来の指標表示を見越したレイアウト設計を含む。
**FRs:** FR1, FR2, FR3

### Epic 3: 財務データ入力と管理 *[Phase 1]*
ユーザーが銘柄ごとに四半期・年度の財務データを入力・修正し、推移を閲覧できるようにする。段階的開示（必須→オプション）で入力負荷を軽減する。
**FRs:** FR8, FR9, FR10, FR11, FR12

### Epic 4: 理論株価算出と計算透明性 *[Phase 1]*
ユーザーが前提パラメータを設定し、理論株価を含む全指標が自動算出され、市場価格との比較や計算ロジックの検証（数式・入力参照・端数処理の閲覧）まで一貫して行えるようにする。銘柄一覧での理論株価表示も含む。
**FRs:** FR4, FR18, FR19, FR20, FR21, FR22, FR23, FR27, FR28

### Epic 5: 財務データ自動取得 — EDINET連携 *[Phase 2]*
システムがEDINET APIと連携して有価証券報告書から財務データを自動取得し、XBRLデータの抽出・マッピング・ユーザー確認フローを提供する。
**FRs:** FR13, FR14, FR15, FR16, FR17, FR29, FR30

### Epic 6: ロースター分類 *[Phase 3]*
ユーザーが銘柄をロースターカテゴリに分類し、分類の変更履歴と理由を記録・閲覧できるようにする。
**FRs:** FR5, FR6

### Epic 7: 銘柄比較ビュー *[Phase 3]*
ユーザーが複数銘柄を横並びに比較し、5段階評価や購入優先順を設定できるようにする。
**FRs:** FR24, FR25, FR26

### Epic 8: 定性分析・AI調査 *[Phase 4]*
システムが銘柄の事業概要やIR資料から事実を整理して提示し、AIによる調査結果を提供する。スコアリングは行わず事実提示に徹する。
**FRs:** FR7, FR31, FR32, FR33

---

## Epic 1: プロジェクト初期化と認証基盤

ユーザーがアカウントを作成してログインし、認証済みのダッシュボードにアクセスできるようにする。プロジェクトの技術基盤（スターターテンプレート、DBスキーマ、RLS、CI/CD）を構築する。

### Story 1.1: プロジェクトスキャフォールドと開発ツール基盤

As a 開発者,
I want プロジェクトの技術基盤（Next.js + Supabase + shadcn/ui）が初期化され、CI/CDパイプラインが動作する状態にしたい,
So that 以降のすべてのストーリーを安全かつ効率的に実装できる。

**Acceptance Criteria:**

**Given** プロジェクトリポジトリが存在する
**When** `create-next-app --example with-supabase` と `npx shadcn@latest init` を実行する
**Then** Next.js + Supabase + shadcn/ui のプロジェクトが生成される
**And** ディレクトリ構成が architecture.md に準拠している（src/app/, src/components/ui/, src/lib/, src/actions/）

**Given** プロジェクトがセットアップされている
**When** GitHub にプッシュする
**Then** GitHub Actions CI が実行され、型チェック + ESLint + ビルドが通る

**Given** 開発環境が構築されている
**When** コードを編集して保存する
**Then** Prettier + Tailwind CSS プラグインによってフォーマットされる

**Given** 新しい開発者がプロジェクトをクローンする
**When** `.env.example` を参照する
**Then** 必要な環境変数（NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY）が確認できる

### Story 1.2: ユーザー登録（サインアップ）

As a 新規ユーザー,
I want メールアドレスとパスワードでアカウントを作成したい,
So that 自分専用の銘柄データを管理できるようになる。（FR34）

**Acceptance Criteria:**

**Given** 未認証のユーザーがサインアップページにアクセスする
**When** メールアドレスとパスワードを入力して送信する
**Then** Supabase Auth でアカウントが作成され、確認メールが送信される

**Given** ユーザーがサインアップフォームを表示している
**When** 無効なメールアドレスまたは短すぎるパスワードを入力する
**Then** onBlur でバリデーションエラーが表示され、aria-describedby でエラーメッセージが関連付けられる

**Given** サインアップフォームが表示されている
**When** キーボードのみで操作する
**Then** すべてのフォーム要素にフォーカスが移動でき、Tab/Enter で操作が完了できる

**Given** 既に登録済みのメールアドレスで登録を試みる
**When** フォームを送信する
**Then** 適切なエラーメッセージが表示される

### Story 1.3: ユーザーログイン・ログアウトとセッション管理

As a 登録済みユーザー,
I want メールアドレスとパスワードでログイン・ログアウトしたい,
So that 安全に自分のデータにアクセスでき、使い終わったらログアウトできる。（FR34）

**Acceptance Criteria:**

**Given** 登録済みユーザーがログインページにアクセスする
**When** 正しい認証情報を入力して送信する
**Then** Cookie ベースのセッションが作成され、ダッシュボードにリダイレクトされる

**Given** 誤った認証情報を入力する
**When** フォームを送信する
**Then** 「メールアドレスまたはパスワードが正しくありません」と表示される（具体的にどちらが間違いかは漏らさない）

**Given** ログイン済みのユーザーがアプリを使用している
**When** ログアウトボタンをクリックする
**Then** セッションが破棄され、ログインページにリダイレクトされる

**Given** 未認証のユーザーが保護されたページにアクセスする
**When** URLを直接入力する
**Then** ログインページにリダイレクトされる

### Story 1.4: 認証済みアプリシェルとレイアウト

As a ログイン済みユーザー,
I want サイドバー付きのアプリレイアウトを見たい,
So that アプリの全体構造を把握し、今後追加される機能に自然にアクセスできる。（FR35）

**Acceptance Criteria:**

**Given** ユーザーがログインしている
**When** ダッシュボードにアクセスする
**Then** サイドバー（240px固定）+ メインコンテンツ領域のレイアウトが表示される

**Given** サイドバーが表示されている
**When** ナビゲーションを確認する
**Then** 「銘柄一覧」へのリンクが表示されている（銘柄リスト部分は「銘柄を登録しましょう」のEmpty Stateガイダンス）

**Given** ウィンドウ幅が768px以下の場合
**When** ページを表示する
**Then** サイドバーが非表示になり、メインコンテンツが全幅で表示される

**Given** アプリシェルが表示されている
**When** キーボードで操作する
**Then** サイドバーのナビゲーション要素にフォーカスが移動でき、フォーカスリングが視認できる

---

## Epic 2: 銘柄管理

ユーザーが投資対象の銘柄を登録・編集・削除し、ウォッチリストとして一覧表示できるようにする。将来の指標表示を見越したレイアウト設計を含む。

### Story 2.1: 銘柄新規登録

As a ログイン済みユーザー,
I want 銘柄コード・企業名・市場・業種・事業セグメントを入力して銘柄を登録したい,
So that 分析対象の銘柄をウォッチリストに追加できる。（FR1）

**Acceptance Criteria:**

**Given** ユーザーがログインしている
**When** 銘柄登録フォームで必要事項（銘柄コード、企業名、市場、業種）を入力して保存する
**Then** `stocks` テーブルに `user_id` 付きでレコードが作成される（RLSにより自分のデータのみアクセス可能）
**And** Toast通知で「銘柄を登録しました」と表示される

**Given** 銘柄登録フォームが表示されている
**When** 必須項目（銘柄コード、企業名）を空のまま送信する
**Then** バリデーションエラーが表示される

**Given** 銘柄登録フォームが表示されている
**When** 既に登録済みの銘柄コードを入力して送信する
**Then** 「この銘柄コードは既に登録されています」とエラーが表示される

**Given** 銘柄登録フォームが表示されている
**When** キーボードのみで操作する
**Then** すべてのフォーム要素にフォーカスが移動でき、Tab/Enter で操作が完了できる

### Story 2.2: 銘柄一覧表示

As a ログイン済みユーザー,
I want 登録した銘柄の一覧を確認したい,
So that 自分のウォッチリストを把握できる。（FR3）

**Acceptance Criteria:**

**Given** ユーザーが1件以上の銘柄を登録している
**When** 銘柄一覧ページにアクセスする
**Then** 登録済み銘柄が一覧で表示される（銘柄コード、企業名、市場、業種）
**And** 将来の理論株価・主要指標表示用のカラムスペースが確保されており、「財務データ入力後に表示」のプレースホルダーが表示される

**Given** ユーザーが銘柄を1件も登録していない
**When** 銘柄一覧ページにアクセスする
**Then** Empty Stateとして「銘柄を登録して分析を始めましょう」のガイダンスと登録ボタンが表示される

**Given** 50件以上の銘柄が登録されている
**When** 銘柄一覧を表示する
**Then** 1秒以内にレンダリングが完了する（NFR2）

**Given** サイドバーが表示されている
**When** 銘柄一覧を確認する
**Then** サイドバーの銘柄リストにも登録済み銘柄が反映されている

### Story 2.3: 銘柄編集・削除

As a ログイン済みユーザー,
I want 登録済み銘柄の情報を編集したり、不要な銘柄を削除したい,
So that ウォッチリストを常に最新の状態に保てる。（FR2）

**Acceptance Criteria:**

**Given** 銘柄一覧に登録済み銘柄が表示されている
**When** 銘柄を選択して編集フォームを開く
**Then** 現在の登録情報（銘柄コード、企業名、市場、業種、事業セグメント）がフォームにプリフィルされる

**Given** 編集フォームで情報を変更する
**When** 保存する
**Then** `stocks` テーブルが更新され、Toast通知で「銘柄情報を更新しました」と表示される
**And** 銘柄一覧とサイドバーに変更が即座に反映される

**Given** 銘柄を削除しようとする
**When** 削除ボタンをクリックする
**Then** 「この銘柄を削除しますか？関連する財務データも削除されます」の確認ダイアログが表示される

**Given** 確認ダイアログで「削除」を選択する
**When** 削除が実行される
**Then** 銘柄とその関連データが削除され、銘柄一覧から消える
**And** Toast通知で「銘柄を削除しました」と表示される

---

## Epic 3: 財務データ入力と管理

ユーザーが銘柄ごとに四半期・年度の財務データを入力・修正し、推移を閲覧できるようにする。段階的開示（必須→オプション）で入力負荷を軽減する。

### Story 3.1: 財務データ入力フォームと保存

As a ログイン済みユーザー,
I want 銘柄ごとに四半期・年度の財務データ（売上、営業利益、純利益、総資産、自己資本等）を入力して保存したい,
So that 理論株価算出に必要なデータを蓄積できる。（FR8, FR12）

**Acceptance Criteria:**

**Given** 銘柄詳細ページの「財務データ」タブを開いている
**When** 新規期間のデータ入力フォームを開く
**Then** 期間属性（年度/四半期、連結/単体）を選択でき、主要勘定科目の入力欄が表示される
**And** 必須項目（売上、営業利益、純利益、総資産、自己資本）が明示され、オプション項目（営業CF、投資CF等）は折りたたみで表示される

**Given** 財務データ入力フォームが表示されている
**When** 金額欄に値を入力する
**Then** 単位選択ドロップダウン（円/千円/百万円/億円）で入力単位を選択でき、保存時に円に統一変換されて `financial_data` テーブルに保存される
**And** 元の入力単位は `input_unit` カラムに記録される

**Given** フォームに入力が完了している
**When** 保存ボタンをクリック、またはCtrl+Sを押す
**Then** `financial_data` テーブルに `user_id` + RLS 付きでレコードが作成され、Toast通知で「財務データを保存しました」と表示される

**Given** 銘柄に財務データが未入力の場合
**When** 「財務データ」タブを開く
**Then** Empty Stateとして「財務データを入力して分析を始めましょう」のガイダンスと入力ボタンが表示される

### Story 3.2: 財務データのバリデーション

As a ログイン済みユーザー,
I want 入力した財務データの妥当性が自動的にチェックされてほしい,
So that 入力ミスに早期に気づき、正確なデータに基づいた分析ができる。（FR11）

**Acceptance Criteria:**

**Given** 財務データ入力フォームで金額欄に入力している
**When** フォーカスが外れる（onBlur）
**Then** Zodスキーマによるバリデーションが実行され、エラーがあればインラインで表示される
**And** エラーメッセージは `aria-describedby` でフォーム要素に関連付けられる

**Given** 数値が期待される入力欄に対して
**When** 文字列や負の値を入力する
**Then** 「有効な数値を入力してください」とバリデーションエラーが表示される

**Given** 同じ銘柄・同じ期間のデータが既に存在する
**When** 同じ期間で新規入力を試みる
**Then** 「この期間のデータは既に登録されています。編集画面から修正してください」とエラーが表示される

**Given** フォームにバリデーションエラーがある状態で
**When** 保存を試みる
**Then** 保存は実行されず、エラー箇所にフォーカスが移動する

### Story 3.3: 財務データの修正

As a ログイン済みユーザー,
I want 入力済みの財務データを修正したい,
So that 入力ミスや更新があった場合にデータを正確に保てる。（FR9）

**Acceptance Criteria:**

**Given** 銘柄に財務データが登録されている
**When** 特定の期間のデータを選択して編集フォームを開く
**Then** 既存の値がフォームにプリフィルされる（保存時の円建て値を元の入力単位に逆変換して表示）

**Given** 編集フォームで値を変更する
**When** 保存する
**Then** `financial_data` テーブルが更新され、Toast通知で「財務データを更新しました」と表示される

**Given** 編集フォームで変更を行っている
**When** 保存せずにページを離れようとする
**Then** 「未保存の変更があります。破棄しますか？」の確認が表示される

### Story 3.4: 財務データ推移の閲覧

As a ログイン済みユーザー,
I want 銘柄ごとの財務データの推移を複数期分まとめて閲覧したい,
So that 企業の業績トレンドを把握できる。（FR10）

**Acceptance Criteria:**

**Given** 銘柄に複数期分の財務データが登録されている
**When** 「財務データ」タブを開く
**Then** 登録済みの全期間のデータが時系列（新しい期間→古い期間）でテーブル形式で表示される
**And** 主要項目（売上、営業利益、純利益、総資産、自己資本）が一目で比較できるレイアウトになっている

**Given** 財務データ推移テーブルが表示されている
**When** スクリーンリーダーで読み上げる
**Then** セマンティックな `<table>` 要素で構成されており、ヘッダーとデータの関係が適切に読み上げられる

**Given** 財務データが1期分のみ登録されている
**When** 推移を表示する
**Then** 1期分のデータが表示され、「複数期のデータを入力すると推移を比較できます」のガイダンスが表示される

---

## Epic 4: 理論株価算出と計算透明性

ユーザーが前提パラメータを設定し、理論株価を含む全指標が自動算出され、市場価格との比較や計算ロジックの検証（数式・入力参照・端数処理の閲覧）まで一貫して行えるようにする。銘柄一覧での理論株価表示も含む。

### Story 4.1: 前提パラメータの設定と管理

As a ログイン済みユーザー,
I want 銘柄ごとに理論株価算出の前提パラメータ（割引率r、成長率g、実効税率等）を設定・調整したい,
So that 自分の投資判断に合った前提条件で理論株価を算出できる。（FR18, FR19）

**Acceptance Criteria:**

**Given** 銘柄詳細ページの「パラメータ」タブを開いている
**When** パラメータ設定画面を表示する
**Then** 各パラメータ（r, g, 実効税率等）にデフォルト値が設定されており、そのデフォルト値の根拠（例：「日本株の長期平均リスクプレミアム」等）が確認できる

**Given** パラメータが未設定の銘柄の場合
**When** 「パラメータ」タブを初めて開く
**Then** `parameters` テーブルにデフォルト値でレコードが作成され、`user_id` + RLS 付きで保存される

**Given** パラメータ設定画面が表示されている
**When** パラメータの値を変更して保存する
**Then** `parameters` テーブルが更新され、Toast通知で「パラメータを保存しました」と表示される

**Given** パラメータ入力欄が表示されている
**When** 不正な値（負の割引率、100%超の税率等）を入力する
**Then** Zodバリデーションによるエラーが表示される

### Story 4.2: 計算エンジンの実装とゴールデンテスト

As a 開発者,
I want 財務データとパラメータから全指標（ROE, PER, 理論株価等）を自動算出する計算エンジンを実装したい,
So that ユーザーに正確で検証済みの理論株価を提供できる。（FR21）

**Acceptance Criteria:**

**Given** `lib/calc/` に計算エンジンの純粋関数が実装されている
**When** 財務データとパラメータを入力として渡す
**Then** 以下の全指標が算出される：自己資本比率、純利益率、売上営業利益率、前年比成長率、営業CF/投資CF/FCF、ROE/ROA/ROIC/移動平均ROIC、PER/PBR/EPS、現状事業価値/現状資産価値、現状理論株価/成長込理論株価、理論PER/理論時価総額/5年後理論時価総額/6年目当期純利益、安全域/安全率、理想購入株価

**Given** 計算エンジンが実装されている
**When** 各計算関数を実行する
**Then** 結果と一緒に透明性メタデータ（数式文字列、入力参照、端数処理ルール、calc_version）が返される

**Given** ゴールデンテスト用データ（1銘柄×3期分）が用意されている
**When** Vitest でゴールデンテストを実行する
**Then** 既存スプレッドシートの計算結果と一致する（差分がある場合は端数処理・単位・期ズレ・入力マッピングのいずれかとして理由が明記される）

**Given** 1銘柄あたりの計算が実行される
**When** 全指標を算出する
**Then** 3秒以内に完了する（NFR1）

### Story 4.3: 理論株価詳細ビュー

As a ログイン済みユーザー,
I want 銘柄ごとに理論株価関連の全指標を一覧で閲覧し、現在の市場価格と比較したい,
So that 投資判断に必要な情報を一目で把握できる。（FR22, FR23）

**Acceptance Criteria:**

**Given** 銘柄に財務データとパラメータが登録されている
**When** 銘柄詳細ページの「理論株価」タブを開く
**Then** 全指標（ROE, PER, 理論株価, 安全域等）が一覧で表示される

**Given** 理論株価が算出されている
**When** 「理論株価」タブを確認する
**Then** 理論株価と現在の市場価格が並べて表示され、乖離率（安全域/安全率）が視覚的に明示される

**Given** 財務データが未入力の銘柄の場合
**When** 「理論株価」タブを開く
**Then** Empty Stateとして「財務データを入力すると理論株価が算出されます」のガイダンスが表示される

**Given** 理論株価ビューが表示されている
**When** スクリーンリーダーで読み上げる
**Then** 各指標のラベルと値が適切に読み上げられる

### Story 4.4: パラメータ変更時のリアルタイム再計算

As a ログイン済みユーザー,
I want パラメータを変更した際に関連する全指標が即座に再計算されて画面に反映されてほしい,
So that パラメータ調整の影響をリアルタイムに確認しながら分析できる。（FR20）

**Acceptance Criteria:**

**Given** 理論株価ビューまたはパラメータタブが表示されている
**When** パラメータ（r, g, 実効税率等）の値を変更して保存する
**Then** 関連する全指標が即座に再計算され、1秒以内に画面に反映される（NFR4）
**And** 変更された指標がインラインハイライトで一時的に強調表示される

**Given** パラメータが変更された後
**When** サイドバーの理論株価サマリーを確認する
**Then** サイドバーの理論株価も再計算後の値に更新されている

### Story 4.5: 銘柄一覧への理論株価・主要指標表示

As a ログイン済みユーザー,
I want 銘柄一覧で各銘柄の理論株価・主要指標を一目で確認したい,
So that 一覧レベルで投資候補の比較ができる。（FR4）

**Acceptance Criteria:**

**Given** 銘柄に財務データとパラメータが登録されている
**When** 銘柄一覧ページを表示する
**Then** Epic 2 で確保したプレースホルダー部分に、理論株価・PER・PBR・安全率等の主要指標が表示される

**Given** 銘柄に財務データが未入力の場合
**When** 銘柄一覧で該当銘柄を確認する
**Then** 指標部分は「—」（ダッシュ）で表示される

**Given** サイドバーが表示されている
**When** 銘柄リストを確認する
**Then** 各銘柄名の横に理論株価サマリー（現状理論株価と安全率）が表示される

### Story 4.6: 計算ロジック透明性（CalcLogicPanel）

As a ログイン済みユーザー,
I want 各指標をクリックして、その算出に使われた数式・入力値・端数処理を確認したい,
So that 計算結果の正確性を自分で検証でき、アプリを信頼して投資判断に活用できる。（FR27, FR28）

**Acceptance Criteria:**

**Given** 理論株価ビューに指標が表示されている
**When** クリッカブル指標（ティール色テキスト + 点線下線で装飾された指標値）をクリックする
**Then** CalcLogicPanel がインラインで展開され、以下が表示される：
- 算出に使用された数式
- 入力値の参照（どの期のどの勘定科目から取得したか）
- 端数処理ルール
- calc_version

**Given** CalcLogicPanel が展開されている
**When** 入力参照リンクをクリックする
**Then** 該当する財務データの入力元（期間・勘定科目）にナビゲートできる

**Given** CalcLogicPanel が展開されている
**When** もう一度指標をクリックするか、パネル外をクリックする
**Then** CalcLogicPanel が閉じる

**Given** クリッカブル指標が表示されている
**When** キーボード（Enter/Space）で操作する
**Then** CalcLogicPanel がマウス操作と同様に展開・閉じる
